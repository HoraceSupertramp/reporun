{
  "private": "true",
  "version": "1.0.0",
  "keywords": [],
  "scripts": {
    "dev": "lerna run dev --stream",
    "start": "lerna run start --stream",
    "build": "lerna run build --stream",
    "deploy": "cd ./packages/reporunner && npm publish || cd ../../"
  },
  "name": "reporun",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "repo_runner": "^1.0.1"
  }
}
